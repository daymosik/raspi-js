import chai from 'chai';
const expect = chai.expect;
const assert = chai.assert;

describe('Johhny test suite', () => {

  beforeEach(() => {});

  afterEach(() => {});

  it('should not fail', () => {
    expect(true).to.be.true;
  });

});
